# Example: serve frontend and proxy /api to backend in production
yourdomain.com {
  encode zstd gzip
  handle_path /api/* {
    reverse_proxy backend:8080
  }
  handle {
    reverse_proxy frontend:80
  }
}
